#include <bits/stdc++.h>
#include <vector>
#define ll long long
#ifdef EVAL
#include "kcolor.h"
#else
#include "grader.cpp"
#endif

using namespace std;

const int M = int(1e9) + 7;

vector<vector<int>> g;

int calculate_colorings(int n, int k, std::vector<std::pair<int, int>> edges,
                        std::vector<bool> mayBeBlack) {

  return 0;
}
